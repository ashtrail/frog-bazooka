extends Node

var score = 0

var GROUP_BONUS = 100
var CHAIN_BONUS = 300

#var Corpse = load("res://entities/corpse/Corpse.tscn")
#
#func spawn_corpse(pos, img, z_offset = 0):
#	var corpse = Corpse.instance()
#	corpse.init(pos, img, z_offset)
#	get_tree().add_child(corpse)
